== RAILS API with User Authentication generated Token multi client and multi roles per client

This repository is a package rails-api with a working simple authentication to generate token,
if you are developing API for multi client and need multi roles for the API you can use this repository
for starting.
